import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Hubungi Kami",
  description: "Hubungi tim representatif PT Yones Satiya Wacana untuk permintaan harga, spesifikasi produk, dan penawaran kerja sama B2B.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
